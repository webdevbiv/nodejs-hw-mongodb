# docs/swagger/components/schemas/AuthResponse.yaml
type: object
required: [accessToken, user]
properties:
  accessToken:
    type: string
  refreshToken:
    type: string
    nullable: true
  user:
    $ref: ./User.yaml
